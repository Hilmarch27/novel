/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com"], // Add any other domains you need
    domains: ["z8qzkzd08p5qad1n.public.blob.vercel-storage.com"]
  },
};

export default nextConfig;
